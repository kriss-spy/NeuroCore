# NeuroCore 改进计划（基于原始考查试卷要求）

> 依据：《2026春季认知工程考查试卷.pdf》题目 2 原文要求

---

## 一、当前状态与差距分析

### 已完成的（✅）
- Baseline：随机 10% 采样 + 冻结 ResNet-18 + MLP 训练 + MSE 报告
- Coreset：时序过滤（预测编码）+ 分布过滤（RAS）+ 统一选择
- Validation：核心集重新训练，对比随机基线，MSE 降低 3.64%
- 报告：中英文版本，包含实验结果与图表

### 关键差距（❌）
1. **语言指令缺失**：试卷明确要求构建 `[视觉特征+语言指令]→[7-DoF动作]`，但当前代码仅使用视觉特征
2. **冗余定义不够突出**：试卷明确要求"重点体现对'冗余'如何定义的独立思考"，当前报告虽有但不够深入
3. **代码注释不足**：试卷要求"清晰的代码注释"，当前注释偏功能说明，缺少脑启发机制解释

---

## 二、Phase 1：语言指令调查与处理（关键优先级）

### 2.1 调查数据集是否包含语言指令

**执行步骤：**
1. 在 notebook 中加载数据集，检查所有字段
2. 查看 Hugging Face 数据集文档确认语言标注情况
3. 如果存在，提取并分析其形式（文本/embedding/任务标签）

**代码：**
```python
from src.data_utils import load_aloha_dataset
ds = load_aloha_dataset()
print(ds.features)  # 查看所有字段
sample = ds[0]
for k, v in sample.items():
    print(f"{k}: {type(v)} {v if not hasattr(v, '__len__') else len(v)}")
```

### 2.2 决策分支

**分支 A：数据集包含语言指令**
- 修改 `src/data_utils.py`：添加语言指令提取函数
- 选择编码方式：
  - 如果是离散任务标签 → one-hot 编码
  - 如果是文本 → 使用预训练文本编码器（如 Sentence-BERT）或简单 embedding
- 修改 `src/baseline.py`：
  - MLP 输入维度从 512 改为 `512 + lang_dim`
  - 在 `build_feature_action_pairs` 中拼接语言特征
- 重新运行 baseline 和 validation

**分支 B：数据集不包含语言指令**
- 在代码中添加显式注释说明
- 在报告中添加设计决策章节：
  > "经检查，ALOHA Sim Transfer Cube 数据集中未提供语言指令标注。根据课程指导（AGENTS.md），在语言标注缺失的情况下，采用纯视觉特征进行降维实验。"
- 保持现有 pipeline 不变

---

## 三、Phase 2：强化"冗余定义"的理论深度（评分重点）

### 3.1 报告结构重组

将"冗余的定义"从 Methodology 的子小节提升为**独立大节**，放在 Methodology 之后、Experiments 之前：

```
3. Methodology
   3.1 Dataset
   3.2 Baseline Architecture
   3.3 Coreset Selection Algorithm
4. Definition of Redundancy  ← 独立大节，评分重点
   4.1 Temporal Redundancy: Predictive Coding Perspective
   4.2 Distributional Redundancy: RAS Perspective
   4.3 Unified Redundancy Score
5. Experiments
```

### 3.2 内容深化

**4.1 时间冗余（预测编码视角）**
- 引用 Millidge et al. 的预测编码理论
- 形式化定义：
  - 帧级别：$\delta_t = ||a_t - a_{t-1}||_2$，若 $\delta_t < \epsilon$ 则该帧时间冗余
  - 片段级别：$R_{temp}(e) = 1 - \frac{S_{temp}(e)}{\max_j S_{temp}(j)}$，冗余度与动作变化量成反比
- 脑启发联系：大脑通过预测编码"解释掉"可预测的输入，只保留预测误差

**4.2 分布冗余（RAS 视角）**
- 引用网状激活系统（RAS）的注意力过滤机制
- 形式化定义：
  - 帧级别：若视觉特征属于已被充分代表的聚类，则该帧分布冗余
  - 片段级别：$R_{dist}(e) = 1 - S_{dist}(e)$，冗余度与视觉多样性成反比
- 脑启发联系：RAS 过滤背景噪音，只让高信息效用的信号进入意识

**4.3 统一冗余度**
- $R_{final}(e) = \alpha \cdot R_{temp}(e) + (1-\alpha) \cdot R_{dist}(e)$
- 核心集 = 冗余度最低的 k 个片段
- **独立思考点**：解释为什么用冗余度（越低越好）而非信息度（越高越好），以及为什么这种双向过滤更符合大脑的层级处理机制

---

## 四、Phase 3：代码注释增强

### 4.1 注释风格要求

每段核心算法必须包含两类注释：
1. **功能注释**：代码做什么（已有）
2. **脑启发注释**：为什么这样做，对应什么脑机制

### 4.2 需要补充注释的位置

**`src/coreset/temporal_filter.py`**
```python
def compute_temporal_scores(dataset):
    """
    预测编码启发的时间过滤。
    
    大脑通过预测编码机制忽略可预测的感官输入，只保留预测误差。
    在机器人数据中，动作变化量小的帧类似于"被预测到的"输入，
    因此信息价值低。我们通过计算每帧动作变化量来量化这种
    "预测误差"，变化量大的片段被认为时间冗余度低。
    
    对应脑机制：Predictive Coding (Millidge et al., 2021)
    """
```

**`src/coreset/distributional_filter.py`**
```python
def compute_distributional_scores(features_dict):
    """
    RAS 启发的分布过滤。
    
    大脑的网状激活系统(RAS)过滤背景噪音，引导注意力聚焦于
    高信息效用的瞬间。在视觉特征空间中，我们使用聚类来识别
    "不同的任务状态"，那些覆盖更多聚类的片段类似于被 RAS
    "注意到"的高效用时刻，因此分布冗余度低。
    
    对应脑机制：Reticular Activating System (RAS)
    """
```

**`src/coreset/select.py`**
```python
def select_coreset(...):
    """
    统一核心集选择：融合预测编码和 RAS 的冗余度评估。
    
    大脑在多个层级上过滤信息：
    1. 底层（预测编码）：过滤时间上的可预测信号
    2. 高层（RAS）：过滤分布上的重复信号
    
    我们的算法模拟这种层级过滤，通过加权融合两种冗余度
    来选择最终的核心集。α 参数控制两个层级的重要性，
    α=0.75 表示预测编码（时间过滤）是主导信号。
    """
```

---

## 五、Phase 4：报告格式与内容完善

### 5.1 报告必须包含的内容（按试卷要求检查）

| 要求 | 状态 | 行动 |
|------|------|------|
| 背景机制调研（预测编码、RAS） | ✅ 有 | 深化理论联系 |
| 核心集选择算法设计思路 | ✅ 有 | 强化冗余定义部分 |
| **对"冗余"如何定义的独立思考** | ⚠️ 有但不够突出 | **重点扩展** |
| 对比实验结果分析 | ✅ 有 | 补充语言指令相关说明 |
| 图文并茂 | ✅ 有 | 确保所有图引用正确 |
| 附上源代码与清晰注释 | ⚠️ 注释不够清晰 | 补充脑启发注释 |

### 5.2 报告结构调整（中文版本）

```
摘要
1. 引言
2. 相关工作与脑机制调研
   2.1 预测编码理论
   2.2 网状激活系统（RAS）
   2.3 数据剪枝与核心集选择
3. 方法论
   3.1 数据集
   3.2 基线架构
   3.3 核心集选择算法
4. 冗余的定义（评分重点）  ← 独立章节
   4.1 时间冗余：预测编码视角
   4.2 分布冗余：RAS 视角
   4.3 统一冗余度与核心集选择
5. 实验
   5.1 基线结果
   5.2 核心集选择结果
   5.3 验证对比
   5.4 消融实验
6. 讨论
7. 结论
参考文献
附录：代码可用性
```

### 5.3 语言指令处理说明

在报告的"数据集"或"方法论"章节中，必须添加一段说明：

```
语言指令说明：
本研究在 ALOHA Sim Transfer Cube 数据集上进行实验。经检查，该数据集
[包含/不包含] 语言指令标注。因此，我们 [采用了语言编码方案 / 采用纯视觉
特征进行降维实验]，这与试卷要求的"[视觉特征+语言指令]→[7-DoF动作]"
框架 [完全一致 / 在特征层面保持一致，语言部分因数据限制暂时省略]。
```

---

## 六、Phase 5：最终检查与提交准备

### 6.1 代码检查清单

- [ ] 语言指令字段已调查并记录结果
- [ ] 所有核心算法文件包含脑启发注释
- [ ] `src/` 目录结构清晰，`notebooks/` 可运行
- [ ] `requirements.txt` 包含所有依赖
- [ ] 代码可在 clean environment 中复现

### 6.2 报告检查清单

- [ ] 中文报告包含"冗余的定义"独立章节
- [ ] 英文报告包含"Definition of Redundancy"独立章节
- [ ] 所有图表有编号、标题和引用
- [ ] 参考文献格式统一
- [ ] 包含代码可用性和复现说明

### 6.3 提交文件清单

```
NeuroCore/
├── 学生名_学号_题号.pdf      ← 中文报告（主要提交物）
├── src/                       ← 源代码（含清晰注释）
│   ├── data_utils.py
│   ├── feature_extractor.py
│   ├── baseline.py
│   ├── validate.py
│   └── coreset/
├── notebooks/                 ← 可选，用于展示
├── results/                   ← 实验结果和图表
└── requirements.txt
```

---

## 七、时间规划建议

| 阶段 | 内容 | 预估时间 |
|------|------|----------|
| Phase 1 | 语言指令调查 + 代码修改 | 2-3 小时 |
| Phase 2 | 报告"冗余定义"章节重写 | 3-4 小时 |
| Phase 3 | 代码注释补充 | 1-2 小时 |
| Phase 4 | 报告格式调整 + PDF 生成 | 2 小时 |
| Phase 5 | 最终检查 | 1 小时 |
| **总计** | | **约 10-12 小时** |

---

## 八、必须避免的事项

根据试卷"不要求每位同学的算法性能一定要达到当前最佳"以及 AGENTS.md 的"避免过度工程化"警告，以下改进**不应执行**：

- ❌ 多随机种子运行和统计显著性检验（数据集太小，无意义）
- ❌ 交叉验证（违反试卷的 held-out 测试集设定）
- ❌ CLIP 特征提取器对比（过度工程化）
- ❌ 超参数网格搜索（简单启发式已足够）
- ❌ Docker / 环境容器（不必要）
- ❌ Word 文档导出（PDF 足够）

**核心原则**：只修复与试卷要求直接相关的差距，不添加额外实验。
